---
// se ejecuta en el servidor o en build time <----
const { title, hasDivider = true } = Astro.props
---

<section>
    {title && <h2>{title}</h2>}
    {hasDivider && <hr class="divider" />}

    <slot />
</section>

<script>
    // se ejecuta en el cliente <----
</script>

<style>
    .divider {
        border: 0;
        border-top: 1px solid #000;
        height: 0;
        width: 100%;
        max-width: 43rem;
        margin: 1rem auto;
    }
    .section-header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 8px;
    }
    section {
        max-width: 43rem;
        margin: 0 auto 12px;
    }

    h2 {
        margin-bottom: 8px;
        font-weight: 700;
        line-height: 1.3;
        font-size: 1.2rem;
    }

    @media (width <= 700px) {
        section {
            margin-bottom: 12px;
        }
    }
</style>
